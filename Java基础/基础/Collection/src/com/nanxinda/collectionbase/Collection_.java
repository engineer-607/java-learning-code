package com.nanxinda.collectionbase;

public class Collection_ {
    /*
    集合：
    1）可以动态保存任意多个对象，使用方便
    2）提供一系列方便的操作对象的方法：add、remove、set、get等
    3）使用集合添加，删除新元素的示意代码-简洁明了

    集合主要分为两种：单列集合，双列集合
    Collection接口有两个重要的子接口List Set，他们的实现子类都是单列集合
    Map接口的实现子类是双列集合，存放的K-V

 * Java集合框架继承关系图 (基于提供的结构)
 * ====================================================
 *
 * 第一层: 顶级接口
 *
 * Iterable<T> (接口)
 * │
 * │  # 所有可迭代对象的基础接口
 * │  # 提供iterator()方法
 * │  # 支持增强for循环
 * │
 * └─────────────────┐
 *                   │
 *                   ▼
 *
 * 第二层: 集合根接口
 *
 * Collection<E> (接口) 继承自 Iterable<E>
 * │
 * │  # 所有集合类的根接口
 * │  # 定义了集合的基本操作:
 * │  #   size(), isEmpty(), add(), remove(), contains()
 * │
 * └─────────────────┐
 *                   │
 *                   ▼
 *
 * 第三层: 主要集合接口
 *
 * List<E> (接口) 继承自 Collection<E>
 * │
 * │  # 有序集合(元素有顺序)
 * │  # 允许重复元素
 * │  # 可以通过索引访问元素
 * │
 * Set<E> (接口) 继承自 Collection<E>
 * │
 * │  # 无序集合(通常)
 * │  # 不允许重复元素
 * │
 * └─────────────────┘
 *
 * 第四层: 具体实现类
 *
 * List接口的实现类:
 *
 * Vector<E> (类) 实现 List<E>
 * │
 * │  # 线程安全的动态数组
 * │  # 同步的(synchronized)
 * │  # 较老的实现(Java 1.0)
 * │
 * ArrayList<E> (类) 实现 List<E>
 * │
 * │  # 非线程安全的动态数组
 * │  # 随机访问快，插入删除慢
 * │  # Java集合框架的主要实现
 * │
 * LinkedList<E> (类) 实现 List<E>
 * │
 * │  # 双向链表实现
 * │  # 插入删除快，随机访问慢
 * │  # 同时实现了Deque接口
 *
 * Set接口的实现类:
 *
 * HashSet<E> (类) 实现 Set<E>
 * │
 * │  # 基于哈希表(HashMap)实现
 * │  # 无序集合
 * │  # 允许null元素
 * │  # 查找效率高
 * │
 * TreeSet<E> (类) 实现 Set<E>
 * │
 * │  # 基于红黑树(TreeMap)实现
 * │  # 有序集合(自然顺序或自定义比较器)
 * │  # 不允许null元素(取决于比较器)
 * │  # 自动排序
 *
 * ====================================================

 * Java Map接口及其实现类继承关系图
 * ======================================================
 *
 * 第一层: Map接口
 *
 * Map<K,V> (接口)
 * ├── 位于: java.util
 * ├── 定义键值对映射的基本操作
 * ├── 主要方法:
 * │   ├── put(K key, V value): V
 * │   ├── get(Object key): V
 * │   ├── remove(Object key): V
 * │   ├── keySet(): Set<K>
 * │   ├── values(): Collection<V>
 * │   ├── entrySet(): Set<Map.Entry<K,V>>
 * │   └── size(): int
 * └── 注意: Map不继承Collection接口，是独立的集合框架
 *
 * 第二层: 直接实现Map接口的抽象类和实现类
 *
 * ┌─────────────────────────────────────┐
 * │      AbstractMap<K,V> (抽象类)       │
 * ├─────────────────────────────────────┤
 * │ 实现: Map<K,V>                       │
 * │ 作用: 提供Map接口的骨架实现           │
 * │ 减少了实现Map接口的工作量             │
 * └───────────────┬─────────────────────┘
 *                 │
 *      ┌──────────┴──────────┐
 *      ▼                     ▼
 * ┌──────────┐        ┌──────────────┐
 * │ Hashtable│        │   HashMap    │
 * ├──────────┤        ├──────────────┤
 * │ 类       │        │ 类           │
 * │ 线程安全  │        │ 非线程安全    │
 * │ 不允许null│        │ 允许null键值 │
 * └──────────┘        └──────┬───────┘
 *      │                     │
 *      │                     ├─────────────────────┐
 *      │                     │                     │
 *      │                     ▼                     ▼
 *      │              ┌──────────────┐     ┌──────────────┐
 *      │              │ LinkedHashMap│     │    TreeMap   │
 *      │              ├──────────────┤     ├──────────────┤
 *      │              │ 类           │     │ 类           │
 *      │              │ 保持插入顺序  │     │ 按键自然排序  │
 *      │              │ 或访问顺序    │     │ 或比较器排序  │
 *      │              └──────────────┘     └──────────────┘
 *      │
 *      │
 * ┌──────────────┐
 * │  Properties  │
 * ├──────────────┤
 * │ 类           │
 * │ Hashtable的子类│
 * │ 用于配置文件  │
 * │ 键值都是String│
 * └──────────────┘

     */
}
